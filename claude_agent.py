"""
Claude agent — delegates to streaming CLI.
Kept as a thin wrapper for backward compatibility.
"""

import logging

logger = logging.getLogger(__name__)


class ClaudeAgent:
    """Placeholder — all work is now done via streaming_cli.py directly."""
    pass
